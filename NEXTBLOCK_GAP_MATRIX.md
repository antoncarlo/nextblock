# NEXTBLOCK_GAP_MATRIX

Baseline dei gap tra lo stato attuale del repository e lo "standard NextBlock" (protocollo istituzionale di tokenizzazione riassicurativa, Base-only, USDC settlement).

- **Data snapshot:** 2026-06-11
- **Metodo:** sola ispezione read-only (find, ls, grep, lettura sorgenti). Nessun file esistente modificato.
- **Chain scope:** Base Sepolia 84532 (staging reale, broadcast 2026-06-10) → Base Mainnet (futuro, fuori scope di questa fase).
- **Convenzione evidenze:** ogni riferimento in `File/evidenza` è stato verificato su disco; ciò che non è stato verificato è marcato `[da verificare]`. Nulla in questa matrice è dichiarato "completo": è una mappa di costruzione.
- **Priorità:** P0 = blocca lo standard/produzione; P1 = necessario prima di mainnet; P2 = consolidamento.
- **Nota ambiente:** `npm 10.9.2` disponibile; `forge` NON installato sulla macchina di snapshot — i comandi Foundry indicati sono eseguibili solo in un ambiente con Foundry.

## Matrice dei gap

| Area | Stato attuale nel codice | File/evidenza | Cosa manca | Cosa va costruito | Priorità | Dipendenze | Criterio di completamento | Comandi di verifica |
|---|---|---|---|---|---|---|---|---|
| Contracts hardening | 18 contratti core implementati con suite unit (19 file), invariant e integration; hardening vault dedicato; Keeper legacy ritirato in Phase 9.5 | `contracts/src/` (18 `.sol`: InsuranceVault, VaultFactory, ClaimManager, VaultAllocator, ProtocolRoles, ComplianceRegistry, NavOracle, BordereauOracle, AIAssessor, AdapterRegistry, PremiumDistributor, PolicyRegistry, PortfolioRegistry, NextBlockLens, ClaimReceipt, VaultDeployer, MockUSDC, MockOracle); `contracts/test/InsuranceVaultHardening.t.sol`; `contracts/test/invariant/VaultInvariant.t.sol`; `contracts/test/integration/FullFlow.t.sol` | `ProtocolTimelock.sol` PRESENTE con 13 test (in main, deployato in staging — vedi riga Governance); nessuna dir `contracts/test/fork/`; `foundry.toml` ha `[profile.ci.fuzz] runs=1000` ma nessun profilo invariant CI; gas snapshot assente; Foundry ora installato anche in locale (forge 1.5.1, suite 370/370 eseguibile prima del push) | Fork test Base Sepolia con blocco pinnato; profilo `[profile.ci]` invariant; `forge snapshot` committato | P1 (timelock consegnato; restano fork test e snapshot) | — | `forge build --sizes` e `forge test -vvv` verdi con suite fork+invariant inclusa | `cd contracts && forge build --sizes && forge test -vvv && ls test/fork` |
| Frontend canonical Lens | MIGRAZIONE ESEGUITA su branch `feat/frontend-canonical-lens` (2026-06-12, 6 commit locali, in attesa di push autorizzato): DepositSidebar legge posizione+compliance LP da `useLensLPStatus`; OracleControls ha card NAV canonica (`useLensOracleDashboard`, nuovo wrapper read-only) e badge `demo-legacy` sul pannello MockOracle; manage page legge vault summary (TVL/UPR/riserve/buffer/fee) da `useLensVaultDashboard`; syndicates page ha sezione vault on-chain reali (factory + `useLensVaultDashboards` batch) e directory illustrativa SENZA metriche finte (rimossi aum/avgApy/tvl/apy/riskProfile/chip KYC dai profili) badgeata `backend-mock`; curator dashboard deriva i vault reali del manager dalla Lens (zero strategie mock, address finto `0xF725...` eliminato anche da my-company). Criterio binario verificato: grep `ACTIVE_STRATEGIES\|avgApy\|0xF725` = 0 match; `aum:` residuo solo come campo form KYB in apply (input utente, non metrica). Consumer DataSourceBadge: 3 -> 8; consumer hook Lens: 2 -> 6. Scritture wagmi, faucet e dati wallet rimasti diretti by design | `app/src/hooks/useNextBlockLens.ts` (6 wrapper: +OracleDashboard, +VaultDashboards batch); `DepositSidebar.tsx`; `OracleControls.tsx`; `vault/[address]/manage/page.tsx`; `syndicates/page.tsx`; `syndicates/dashboard/page.tsx`; `my-company/page.tsx` | Push + CI + merge del branch (autorizzazione owner); stati async indicizzati da eventi/indexer (dipende dal P0 API/indexer); viste claim/premium/adapter/bordereau della Lens ancora senza consumer UI (parte del P0 claim lifecycle UI) | Merge del branch; consumi Lens per claim/premium/adapter nelle viste dedicate quando si fara' claim lifecycle UI | P1 (declassato da P0: viste core migrate, restano i consumer claim/premium legati ad altri P0) | NextBlockLens deployata (fatto); merge autorizzato | Zero metriche di protocollo finte non etichettate nelle viste (raggiunto); ogni sezione dati con DataSourceBadge (raggiunto per le viste in perimetro); lint/typecheck/build/addressbook verdi (verificato in locale) | `grep -rn "ACTIVE_STRATEGIES\|avgApy\|0xF725" app/src/app app/src/components --include=*.tsx` deve dare 0; `npm run ci:frontend:*` e `npm run check:addressbook` alla root |
| Dependency hardening | Tier 1+2 COMPLETATO E MERGIATO (2026-06-12): PR #1 merged, `main` = merge commit `253f1558`, run CI su main 27337157313 verde (3/3 job); inclusi i fix owner per il deploy Vercel (`705e77a` script build root, `011484a` `.npmrc` root); deployment Vercel `nextblock2` completato dall'owner. Dettaglio del lavoro: viem 2.45.1→2.52.2, rainbowkit 2.2.10→2.2.11, next/eslint-config-next 16.1.6→16.2.9, wagmi mantenuto 2.19.5 (no major); `npm audit fix` non-force applicato. Audit `--omit=dev`: da 37 (29 moderate, 8 high) a 23 (23 moderate, 0 high, 0 critical) — `npm audit --audit-level=high --omit=dev` exit 0. Residuo: 23 moderate tutte transitive della catena wallet (WalletConnect/Reown/MetaMask/Solana via RainbowKit, piu' next/viem/wagmi flaggati con fix solo major/downgrade, non azionabile). Lint/typecheck/build/addressbook verdi post-bump | `app/package.json` e `app/package-lock.json` sul branch; output audit before/after in copia isolata (2026-06-11) | Decisione owner sul residuo moderate: accettazione temporanea (consigliata), override mirati `@walletconnect/*`>=2.22.5 / `@reown/appkit`>=1.9 (rischio mismatch interno catena Reown, da smoke-testare), oppure step separato wallet stack upgrade (wagmi 3.x major). Reown projectId: RISOLTO (2026-06-12) — `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` configurato dall'owner come env var su Vercel (valore mai committato nel repo), redeploy `nextblock2` Ready, verifica su https://www.nextblock.finance/app con endpoint Reown/WalletConnect 200/202 e zero 403 | Step dedicato wallet stack (override o wagmi 3.x) solo se non si accetta il residuo moderate | P2 (high/critical azzerati, merge fatto, projectId configurato) | Decisione owner su residuo moderate | `npm audit --audit-level=high --omit=dev` exit 0 (raggiunto); livello moderate exit 0 solo dopo wallet stack upgrade o override accettati | copia isolata di `app/package*.json` + `npm audit --omit=dev`; `npm run ci:frontend:*` e `npm run check:addressbook` alla root |
| Wallet smoke test | Nessun test e2e/smoke automatizzato nel repo; verifica wallet solo manuale `[da verificare]` | Assenza verificata: nessun config Playwright/Cypress in `app/` (find depth 2: solo config Next/ESLint/PostCSS/TS) | Suite smoke: connect (Coinbase Smart Wallet + EOA fallback), switch a 84532, deposit/withdraw su vault staging | Script e2e ripetibile contro Base Sepolia staging con wallet di test | P1 | Dependency hardening; staging stabile | Lo smoke test passa in modo ripetibile contro lo staging 84532 | `cd app && npm run test:e2e` (script da creare) |
| SDK | Inesistente: repo app-centrico con un solo `package.json` in `app/`; nessuna dir `packages/` | `find . -maxdepth 2 -name package.json` → solo `app/package.json`; root contiene solo `app/`, `contracts/`, `src/` | Package SDK TypeScript (viem-based) con ABI tipizzate, address book importato, helper transaction builder | `packages/sdk` con client read (Lens) e write (vault/claim/premium), test, build | P1 | Root workspace (npm workspaces); address book codegen | `packages/sdk` compila con test verdi ed è consumato da `app/` al posto degli ABI inline | `npm -w packages/sdk run build && npm -w packages/sdk test` (post-workspace) |
| API/indexer | Inesistente: nessun servizio API né indexer eventi; presente solo client Supabase non collegato al protocollo | `app/src/integrations/supabase/client.ts`, `app/src/integrations/supabase/types.ts`; nessuna dir api/indexer alla root; copia legacy in `src/integrations/supabase/` (Vite app residua) | Indexer eventi (HyperSync/Goldsky/The Graph) e API mirror per stati async (Submitted/Assessed/Approved/Queued/Settled…) | Package indexer + API; il frontend smette di inferire lo stato dal solo receipt | P1 | SDK; root workspace | Le viste lifecycle leggono stati indicizzati da eventi, non dal receipt della tx | `curl <api>/health` e query stati claim/portfolio da indexer (post-build) |
| KYB/KYC backend | P0A IMPLEMENTATO IN LOCALE su branch `feat/kyb-backend` (2026-06-12), MIGRATION REMOTA NON APPLICATA (pending review SQL + autorizzazione owner): schema zod + macchina a stati (`submitted/under_review/approved/rejected/needs_info`) con smoke test node strip-types 18/18; migration versionata `supabase/migrations/0001_kyb_applications.sql` (kyb_applications + kyb_review_events append-only, RLS deny-by-default, indici, zero seed); `supabase-server.ts` service-role fail-closed (503 se env mancante); route API: POST submit validato, GET lista operator-only, POST review con transizione+audit event, GET status minimale senza PII; auth operator = firma EIP-191 + finestra 300s + verifica ON-CHAIN server-side di KYC_OPERATOR_ROLE/OWNER_ROLE via viem (read-only, fail-closed); `apply/page.tsx` con submit reale e stati espliciti (zero "backend mock"); `KybReviewQueue` in admin (lista, transizioni con nota, audit trail, calldata `setWhitelist` futura MAI inviata). L'approvazione DB resta istruttoria: la whitelist on-chain e' atto separato del KYC Operator via Safe | `supabase/migrations/0001_kyb_applications.sql`; `app/src/lib/kyb/schema.ts`; `app/src/lib/kyb/auth.ts`; `app/src/lib/supabase-server.ts`; `app/src/app/api/kyb/**`; `app/src/app/app/apply/page.tsx`; `app/src/components/admin/KybReviewQueue.tsx`; `app/scripts/kyb-smoke.ts`; `app/.env.example` | Review SQL owner + `apply_migration` autorizzato; configurazione `SUPABASE_SERVICE_ROLE_KEY` su Vercel (mai committata); rigenerazione tipi Supabase post-migration; smoke end-to-end submit/review su staging; nonce single-use per le review COMPLETATO (2026-06-13, PR #10: store server-side + /api/kyb/auth/nonce + nonce nel messaggio firmato, consumato alla verifica; rate limiting IP su submit/review/nonce); restano upload documenti (bucket privato) e whitelist on-chain via Safe | Step autorizzativo: apply migration + env + smoke E2E; poi step on-chain whitelist separato | P0 (sblocco = autorizzazione migration) | Owner: review SQL, env Vercel; Governance per l'atto on-chain | Submit dal frontend persiste riga `submitted` verificabile; review non autorizzata respinta 401/403; audit trail append-only popolato; on-chain whitelist verificabile via `cast call <complianceRegistry> "whitelisted(address)(bool)"` solo dopo l'atto Safe separato | `node --experimental-strip-types app/scripts/kyb-smoke.ts`; `npm run ci:frontend:*`; grep "backend mock" su apply = 0; grep SERVICE_ROLE fuori da file server/env.example = 0 |
| Claim lifecycle UI | Hook claim presenti e ClaimManager deployato; copertura UI dell'intero lifecycle istituzionale (Submitted→Disputed→Approved→Settled) parziale `[da verificare]`; controlli oracle admin contengono mock | `app/src/hooks/useClaimReceipts.ts`, `app/src/hooks/useClaimTrigger.ts`; `contracts/src/ClaimManager.sol`; `app/src/components/admin/OracleControls.tsx` (mock) | Viste per Cedant (submit/track), Claims Committee (approve), Sentinel (dispute) con stati indicizzati e finestre liveness visibili | Dashboard claim role-based wirata a ClaimManager + Lens + indexer | P1 | Frontend canonical Lens; API/indexer | Ogni stato del lifecycle claim è visibile in UI da eventi indicizzati, incluse finestre di dispute | `grep -rn "ClaimStatus\|dispute" app/src --include=*.tsx` + walkthrough flusso su staging |
| Bordereau finalization | Contratto, test unit e deploy staging presenti; automazione della finalization post-liveness e UI assertion/dispute assenti `[da verificare]` | `contracts/src/BordereauOracle.sol`; `contracts/test/BordereauOracle.t.sol`; `bordereauOracle: 0xaB07…7Ed0` in `contracts/deployments/84532-staging.json` | Esecutore della finalization a liveness scaduta; UI per assertion, challenge e stato | Task keeper "finalize bordereau" + vista dedicata negli admin/cedant dashboard | P2 | Bot/keeper; claim lifecycle UI | Un'assertion sottomessa in staging viene finalizzata automaticamente a liveness scaduta ed è visibile in UI | `cast call <bordereauOracle> <getter assertion> --rpc-url base_sepolia` (getter esatto `[da verificare]`) |
| Bot/keeper | Ritirato esplicitamente: `Keeper.s.sol` è uno stub che logga il ritiro (Phase 9.5, dipendeva dai trigger legacy rimossi); `allocatorBot` in staging è lo stesso EOA del deployer, nessun processo automatico | `contracts/script/Keeper.s.sol` (header "RETIRED — Phase 9.5 security hardening"); `allocatorBot` in `contracts/deployments/84532-staging.json` = `0xfF6f…81d2` | Keeper off-chain per `ClaimManager.executeClaim` (solo claim APPROVED a finestra chiusa) e allocator bot bound ai limiti on-chain | Package bot TS (SDK-based) con key segregata, retry, alerting; nessuna autorità oltre i ruoli on-chain | P1 | SDK; governance (key/ruolo dedicato) | Bot esegue su staging solo azioni permesse dal proprio ruolo, con log verificabili | esecuzione bot su staging + `cast` di verifica dello stato post-azione |
| NAV/risk oracle | NavOracle e MockOracle deployati in staging; `oracleNode` = stesso EOA deployer; nessun adapter Braino.ai/WAVENURE reale né updater automatico; stale-check operativi non esercitati `[da verificare]` | `contracts/src/NavOracle.sol`; `contracts/src/MockOracle.sol`; `navOracle`, `oracleNode` in `contracts/deployments/84532-staging.json` | Adapter NAV reale (Braino.ai/WAVENURE) dietro interfaccia; processo di update firmato e schedulato; reazione Sentinel a NAV stale/deviante | Servizio oracle-updater + runbook stale-NAV + test fork del path stale | P1 | Bot/keeper; governance | NAV aggiornato in staging da nodo dedicato (non deployer); stale-check dimostrato con test/fork | `cast call <navOracle> <getter NAV/lastUpdate> --rpc-url base_sepolia` (getter esatto `[da verificare]`) |
| Governance Safe/timelock | FASE 1 COMPLETATA ON-CHAIN (broadcast 2026-06-11, autorizzato): `ProtocolTimelock` deployato su Base Sepolia a `0x6e2927627d83A90EDC9cDA3c626B49875f9449CF` (minDelay 3600s = floor; bytecode 6567 byte), Safe `0x8Fd8b45Ba2612E7535bbeB21615554701CfaF870` proposer+executor+canceller, timelock self-administered; `OWNER_ROLE` e `DEFAULT_ADMIN_ROLE` su ProtocolRoles concessi al timelock (tx `0x67124971...` deploy, `0xe5c7f612...` grant OWNER, `0x1d8e8ea1...` grant ADMIN; blocchi 42710589/91/93, tutte success). Verificato DUE volte: post-broadcast owner + verifica indipendente via cast. Il deployer `0xfF6f...81d2` CONSERVA OWNER_ROLE e DEFAULT_ADMIN_ROLE: intenzionale (fase 1, nessuna revoca prematura). Codice: `ProtocolTimelock.sol` + 13 test in main; script migrazione a due fasi | `contracts/src/ProtocolTimelock.sol`; `contracts/test/ProtocolTimelock.t.sol`; `contracts/script/GovernanceMigration.s.sol`; `contracts/deployments/84532-staging.json` (chiavi `protocolTimelock` e `safe`); verifiche on-chain via cast su `https://sepolia.base.org` | Prova generale di un'operazione timelockata via Safe (schedule -> attesa 1h -> execute); fase 2 `RENOUNCE_DEPLOYER` (BLOCCATA fino a nuova autorizzazione esplicita e solo dopo prova generale riuscita); separazione key operative (sentinel, kycOperator, oracleNode, allocatorBot); esposizione governance addresses in codegen/frontend; documentazione poteri/limiti per ruolo | Prova generale timelockata; poi fase 2 renounce autorizzata; estensione codegen | P0 (declassabile a P1 dopo fase 2) | Autorizzazione esplicita owner per ogni operazione on-chain | Nessun ruolo critico su singolo EOA nello staging; ogni azione risk-increasing passa da schedule->delay->execute | `cast call 0xEE93...c976 "hasRole(bytes32,address)(bool)" $(cast keccak "OWNER_ROLE") 0x6e29...49CF --rpc-url https://sepolia.base.org` (atteso true); idem per deployer (atteso false solo dopo fase 2) |
| Address book/deployments | Baseline codegen attiva e consumata (aggiornamento 2026-06-11): `scripts/generate-addressbook.mjs` genera `app/src/config/generated/addressBook.ts` (deterministico, indirizzi validati ed emessi verbatim in checksum EIP-55) da `contracts/deployments/84532-staging.json`; `scripts/check-addressbook.mjs` fa anti-drift byte-for-byte in CI (job `addressbook`). Il blocco runtime Base Sepolia 84532 in `app/src/config/contracts.ts` ora importa `NEXTBLOCK_ADDRESSES`/`NEXTBLOCK_CHAIN_ID` dal modulo generato (mapping chiavi: mockUSDC <- usdc, nextBlockLens <- lens): nessun indirizzo di contratto staging hardcoded a mano nel frontend | `contracts/deployments/84532-staging.json`; `scripts/generate-addressbook.mjs`; `scripts/check-addressbook.mjs`; `app/src/config/generated/addressBook.ts`; `app/src/config/contracts.ts` (import da `./generated/addressBook`, blocco `[NEXTBLOCK_CHAIN_ID]`) | Follow-up residui NON toccati in questo step: (a) ABI inline in `contracts.ts` non ancora generati da codegen; (b) `ADMIN_ADDRESSES` in `app/src/config/constants.ts` usa un address admin non presente nel deployment JSON, quindi non derivabile dall'address book — va risolto col lavoro governance/ruoli; (c) blocchi chain legacy (31337, 11155111, arc) restano manuali by design; (d) consumo dal futuro SDK | Estensione del codegen agli ABI; address book consumato dal futuro SDK; razionalizzazione del gate admin lato governance | P2 | CI/CD (fatto per il check); SDK (per il consumo lato SDK); Governance (per il gate admin) | Config frontend/SDK generata, non scritta a mano; CI fallisce su drift (attivo); nessun indirizzo hardcoded duplicato nei consumer (raggiunto per gli indirizzi contratti staging 84532) | `npm run codegen:addressbook && npm run check:addressbook`; `grep -rn "0x7C56F056\|0x4E587AB5" app/src --include=*.ts --include=*.tsx` deve restituire solo `generated/addressBook.ts` |
| CI/CD | CI minima creata (2026-06-11): `package.json` root con workspace `app` e script `ci:*` che delegano a frontend (`npm --prefix app`) e contratti (`forge`); workflow `.github/workflows/ci.yml` con job separati `addressbook` (anti-drift address book, aggiunto 2026-06-11), `frontend` (Node 22: install da lockfile, lint, typecheck, build) e `contracts` (Foundry: fmt --check, build --sizes, test -vvv). Repository git inizializzato in locale su branch `main` con primo commit (2026-06-11); remote GitHub, push e prima esecuzione della pipeline in attesa di autorizzazione (campo operativo remote non compilato). CI GitHub VERDE (2026-06-11): repository pubblicato su `https://github.com/antoncarlo/nextblock` (origin, main, push fast-forward senza force, history legacy 2026-02-27 preservata come ancestry). Prima run 27313648087 (commit `859690d`): addressbook SUCCESS, frontend e contracts FAILURE. Regression fix applicato in `2f6af42` (job frontend ancorato agli script root `npm --prefix app`, niente risalita al workspace root senza lockfile) e `beb1daa` (`forge fmt` su 34 file, behavior-preserving: suite locale 357/357 test passati post-fmt con forge 1.5.1 installato in locale). Run di verifica https://github.com/antoncarlo/nextblock/actions/runs/27314071700 (commit `beb1daa`): job `addressbook` SUCCESS, job `frontend` SUCCESS (install/lint/typecheck/build), job `contracts` SUCCESS (fmt check, build --sizes, test -vvv) — submodule Foundry pinnati accettati dalla CI | `.github/workflows/ci.yml`; `.gitmodules`; run 27314071700 (verde); `contracts/` formattato forge fmt | Audit gate npm in CI; `forge snapshot` e profilo invariant CI; branch protection su main; badge CI nel futuro README root | Estensione workflow (gas snapshot, audit gate) e branch protection in uno step dedicato | P1 (declassato da P0: pipeline base verde) | — | Ogni push esegue la CI completa su GitHub con entrambi i job verdi; una PR con suite rossa non è mergeabile | `npm run ci` alla root (richiede Foundry per la parte contracts); `gh run list` dopo il push |
| Docs/runbook | BASELINE CREATA (2026-06-12) e COMPLETATA (2026-06-13, branch docs/legal-compliance-finalization): README root, SECURITY.md, CONTRIBUTING.md, docs/SECURITY_MODEL.md, audits/README.md (con Audit Request Checklist); docs/OPERATIONS.md ora include PRODUCTION BLOCK governance e Incident Response Runbook completo (IR-1 frontend outage con rollback Vercel, IR-2 RPC failure con fallback e probe, IR-3 pause Sentinel con leve per contratto e comandi cast, IR-4 emergenza Safe/timelock con cancel e criteri di autorizzazione, IR-5 key rotation SUPABASE_SERVICE_ROLE_KEY e WALLETCONNECT_PROJECT_ID); AGGIUNTI (2026-06-13) docs/TERMS.md e docs/PRIVACY.md completi e docs/LEGAL.md (struttura NextBlock/Klapton Re/Braino, risk disclosure depositanti, sintesi KYC/AML), linkati dal footer. Resta: walkthrough operativo provato da un operatore terzo (prova pratica) e review legale qualificata pre-mainnet | `README.md`; `SECURITY.md`; `CONTRIBUTING.md`; `docs/SECURITY_MODEL.md`; `docs/OPERATIONS.md`; `docs/TERMS.md`; `docs/PRIVACY.md`; `docs/LEGAL.md`; `audits/README.md` | Walkthrough operativo provato da un operatore terzo; review da counsel qualificato prima del mainnet | Validazione runbook con prova pratica; ingaggio counsel | P2 | — | Un operatore nuovo esegue le operazioni base e gli scenari IR seguendo solo i docs; counsel approva i documenti legali | `ls docs/TERMS.md docs/PRIVACY.md docs/LEGAL.md && grep -c "IR-" docs/OPERATIONS.md` |
| Audit readiness | MIGLIORATA (2026-06-12): legacy root `src/` (94 file, app Vite residua con client Supabase hardcoded) RIMOSSO dal codice attivo; gas snapshot baseline committata (`contracts/.gas-snapshot`, 369 entry) con check `npm run audit:contracts:snapshot`; script coverage (`audit:contracts:coverage`); profilo `[profile.ci.invariant]` e seed fuzz fissato in foundry.toml; audits/README.md con stato onesto e gap list. Restano: fork test (`test/fork/` assente), static analysis (slither/aderyn), soglie coverage, threat model per modulo | `audits/README.md`; `contracts/.gas-snapshot`; `contracts/foundry.toml`; commit di rimozione legacy | Fork test Base Sepolia pinnati; slither/aderyn triagiato; soglie coverage; fase 2 governance completata prima dell'audit esterno | Step dedicato fork-tests + static analysis | P1 | Contracts hardening; CI/CD | Coverage misurata e pubblicata; zero findings statici high non triagiati; nessun secret in chiaro nel repo | `npm run audit:contracts:snapshot && npm run audit:contracts:coverage`; `cd contracts && slither .` (tool da installare) |

## Note di baseline

1. **Legacy rimosso (2026-06-12):** la dir `src/` alla root (app Vite residua pre-Next, 94 file incluso un client Supabase hardcoded) e' stata eliminata dal codice attivo nel readiness pass dopo verifica grep di zero import attivi; resta recuperabile dalla history git.
2. **Single-EOA staging:** lo staging 84532 è reale ma interamente operato dal deployer `0xfF6f…81d2` su tutti i ruoli. È accettabile per demo, è il gap più critico verso lo standard (riga Governance).
3. **`Keeper.s.sol` esiste ma è ritirato:** il file è presente come stub documentale del ritiro Phase 9.5; non c'è alcun keeper operativo.
4. **Foundry non presente sulla macchina di snapshot:** i comandi `forge`/`cast` di questa matrice sono eseguibili solo in ambiente con Foundry installato.
5. **Nessuna riga di questa matrice è "done":** anche le aree con stato avanzato (contracts, frontend Lens) restano aperte finché il rispettivo criterio di completamento binario non è verificato.
6. **CI attiva e verde su GitHub Actions (aggiornamento 2026-06-11):** repository pubblicato su `https://github.com/antoncarlo/nextblock` (main), pipeline a tre job (`addressbook`, `frontend`, `contracts`) interamente verde dalla run 27314071700. Dipendenze Foundry come submodule pinnati (forge-std v1.9.7, openzeppelin-contracts v5.4.0). Foundry e' ora installato anche in locale (forge 1.5.1): l'intera suite contracts (357 test) e' verificabile prima del push. Verifica locale: script `npm run ci:frontend:*`, `npm run ci:contracts:*` (con `~/.foundry/bin` nel PATH) e `npm run check:addressbook` alla root.
