{
  "name": "nextblock-redemption-indexer",
  "version": "0.1.0",
  "description": "NextBlock RedemptionQueue subgraph (Base Sepolia) — LP exit lifecycle history.",
  "author": "Anton Carlo Santoro",
  "license": "MIT",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goldsky": "goldsky subgraph deploy nextblock-redemption/v1 --path .",
    "deploy:studio": "graph deploy --studio nextblock-redemption"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
